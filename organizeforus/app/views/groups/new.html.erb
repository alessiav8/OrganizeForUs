<%if !user_signed_in? %>
  <% controller.redirect_to new_user_session_path %>
<%end%>

<h1 class="display-6">Create new group</h1>

<%= render "form", group: @group %>

<br>

<div>
  <%= link_to "Back to your groups", groups_path, class:"btn btn-secondary" %>
</div>
